name: Release 1.5.1
on:
  push:
    branches: [main]
    paths: [.github/workflows/release-1.5.1.yml]
permissions:
  contents: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate PHP and release version
        run: |
          find includes -name '*.php' -print0 | xargs -0 -n1 php -l
          php -l lv-news-suite.php
          php -l tests/counters.php
          grep -F 'Version: 1.5.1' lv-news-suite.php
          grep -F "define('LV_NEWS_SUITE_VERSION', '1.5.1');" lv-news-suite.php
          grep -F 'Stable tag: 1.5.1' readme.txt
          grep -F "add_action('wp_footer', [__CLASS__, 'output'], 100);" includes/class-lv-news-counters.php
          ! grep -F 'class="lv-news-counters"' includes/class-lv-news-counters.php
      - name: Build installable plugin
        run: python3 tools/build-release.py
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create v1.5.1 dist/lv-news-suite-1.5.1.zip dist/lv-news-suite-1.5.1.sha256 \
            --target "$GITHUB_SHA" --title 'LV News Suite 1.5.1' \
            --notes-file docs/RELEASE-NOTES-1.5.1.md
